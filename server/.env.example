# ── MongoDB ──────────────────────────────────────────────
MONGO_URI=mongodb://localhost:27017/schedule_tracker

# ── Server ───────────────────────────────────────────────
PORT=8420
CLIENT_ORIGIN=http://localhost:5000
SESSION_SECRET=a3f8c2d914e76b0f582937ac1d4e8f30b76c2a514d8e93f0127c4b8d63a9e15f

# ── Feature passwords (same defaults as the Flask app) ──
DELETE_PASSWORD=delete@vs2026
UPDATE_PASSWORD=update@123
LOG_PASSWORD=log@vs2005
REQLOG_PASSWORD=req@logvs123

# ── Business constants ───────────────────────────────────
COMPLETED_DAYS_GRACE=5
COMPLETED_DAYS_GRACE_EXTENDED=15
DUE_SOON_DAYS=3

# ── File storage (local disk) ────────────────────────────
UPLOAD_DIR=./uploads
BACKUP_DIR=./uploads/backups
BACKUP_RETENTION_DAYS=60

# ── PostgreSQL source (only needed to run the one-time migration) ──
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=new_schedule_track
PG_USER=postgres
PG_PASSWORD=changeme
